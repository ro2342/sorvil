<Page
    x:Class="Sorvil.Views.BookDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <StackPanel Padding="20,24,20,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                        Width="110" Height="160"
                        HorizontalAlignment="Left">
                    <Image x:Name="CoverImage" Stretch="Uniform" />
                </Border>

                <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Top">
                    <TextBlock x:Name="TitleText" Style="{StaticResource SubtitleTextBlockStyle}" TextWrapping="Wrap" />
                    <TextBlock x:Name="AuthorText" Style="{StaticResource BodyTextBlockStyle}" TextWrapping="Wrap" Margin="0,4,0,0" />
                </StackPanel>
            </Grid>

            <ItemsControl x:Name="FormatsList" Margin="0,20,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0"
                                    Content="{Binding PrimaryLabel, Mode=OneWay}"
                                    Click="PrimaryButton_Click"
                                    Tag="{Binding}"
                                    HorizontalAlignment="Stretch" />
                            <Button Grid.Column="1"
                                    Content="Apagar"
                                    Click="DeleteButton_Click"
                                    Tag="{Binding}"
                                    Visibility="{Binding DeleteVisibility, Mode=OneWay}"
                                    Margin="8,0,0,0" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <ProgressBar x:Name="DownloadProgress"
                         Minimum="0" Maximum="100"
                         Visibility="Collapsed"
                         Margin="0,12,0,0" />

            <TextBlock x:Name="StatusText"
                       Style="{StaticResource BodyTextBlockStyle}"
                       TextWrapping="Wrap"
                       Margin="0,12,0,0" />

            <TextBlock x:Name="SummaryText"
                       Style="{StaticResource BodyTextBlockStyle}"
                       TextWrapping="Wrap"
                       Margin="0,20,0,0" />
        </StackPanel>
    </ScrollViewer>
</Page>
